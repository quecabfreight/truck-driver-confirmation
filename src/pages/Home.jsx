import React from "react";

function Home() {
  return (
    <div className="w-full min-h-screen flex items-center justify-center text-gray-200 text-center p-8">
      <div className="text-sm text-gray-400 tracking-wide leading-relaxed max-w-sm">
        <div className="text-xl font-semibold text-gray-100 mb-2">
          QueCab <span className="text-red-500 font-bold">AdbS</span>
        </div>
        Logged in (placeholder). This will become the live dashboard /
        verification console.
      </div>
    </div>
  );
}

export default Home;
